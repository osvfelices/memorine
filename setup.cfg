[metadata]
name = memorine
version = 0.2.0
description = Human-like memory for AI agents. No embeddings, no LLMs, no cost.
author = osvfelices
url = https://github.com/osvfelices/memorine
license = MIT

[options]
packages = find:
python_requires = >=3.9

[options.packages.find]
include = memorine*
